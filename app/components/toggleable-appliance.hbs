<div class="flex items-center px-4 py-2">
  <span class="text-gray-400 mr-8 w-40">{{@appliance.name}}</span>
  {{#if @appliance.board}}
    <Ui::ToggleSwitch @on={{this.on}} @onToggle={{perform this.toggle}} />
  {{else}}
    No board connected
  {{/if}}
</div>
